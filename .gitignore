node_modules
dist
dist-ssr
# Secretos: NADA de .env entra al repo (este repositorio es público).
.env
.env.*
!.env.example
.env.local
.env.*.local
.netlify
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json

# Artefacto del build del server (npm run build:server) — se genera, no se versiona.
server/index.mjs
